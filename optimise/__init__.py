"""PCM Season Planner — optimiser package.

Reads the local planner SQLite database and produces rider-race assignments
for the season.

Run with:
    python -m optimise --help
    python -m optimise --database data/planner.sqlite
"""
