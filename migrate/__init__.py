"""PCM Season Planner — Lachis-to-planner migration package.

Imports data from a Lachis Editor XML export into the local planner SQLite
database, optionally enriching stage rows with metadata from CTStageEditor.exe.

Run with:
    python -m migrate --help
"""
